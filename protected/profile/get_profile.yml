info:
  name: get_profile
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/profile
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
