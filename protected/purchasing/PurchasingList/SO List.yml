info:
  name: SO List
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/purchasing/so_purchase_list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
