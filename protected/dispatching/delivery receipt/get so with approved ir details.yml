info:
  name: get so with approved ir details
  type: http
  seq: 7

http:
  method: GET
  url: http://{{host}}:{{port}}/api/delivery-receipts/so-with-approved-ir-details/1
  body:
    type: text
    data: |-
      {
          "ship_name": "Truckssss1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
