info:
  name: get so with approved ir
  type: http
  seq: 6

http:
  method: GET
  url: http://{{host}}:{{port}}/api/delivery-receipts/so-with-approved-ir/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
