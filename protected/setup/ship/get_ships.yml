info:
  name: get_ships
  type: http
  seq: 3

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/shiptype
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
