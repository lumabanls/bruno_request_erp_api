info:
  name: create_ship
  type: http
  seq: 5

http:
  method: POST
  url: http://{{host}}:{{port}}/api/setup/shiptype
  body:
    type: json
    data: |-
      {
          "ship_name": "Truckssss1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
