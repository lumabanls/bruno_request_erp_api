info:
  name: get_ship
  type: http
  seq: 4

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/item/shiptype
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
