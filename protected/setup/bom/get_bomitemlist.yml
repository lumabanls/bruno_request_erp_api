info:
  name: get_bomitemlist
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/bom/item_list
  body:
    type: json
    data: "\n\n"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
