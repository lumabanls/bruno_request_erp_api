info:
  name: get_items
  type: http
  seq: 11

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/item
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
