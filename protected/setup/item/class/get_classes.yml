info:
  name: get_classes
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:/api/setup/item/class
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
