info:
  name: update_pump_count
  type: http
  seq: 4

http:
  method: PUT
  url: http://{{host}}:{{port}}/api/setup/item/pump_count
  body:
    type: json
    data: |-
      {
          "id": 2,
          "code": "CODE-331",
          "name": "UPDATED TYPE COUNT"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
