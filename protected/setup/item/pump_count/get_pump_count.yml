info:
  name: get_pump_count
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/item/pump_count
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
