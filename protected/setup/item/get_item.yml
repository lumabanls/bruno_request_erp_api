info:
  name: get_item
  type: http
  seq: 12

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/item/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
