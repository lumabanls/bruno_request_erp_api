info:
  name: get_usetypes
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/warehouse/usetype
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
