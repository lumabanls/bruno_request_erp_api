info:
  name: InventoryName_get Copy
  type: http
  seq: 6

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/inv/name
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
