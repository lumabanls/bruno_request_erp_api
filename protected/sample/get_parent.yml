info:
  name: get_parent
  type: http
  seq: 4

http:
  method: GET
  url: http://{{host}}:{{port}}/api/sample/parent/10002
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
