info:
  name: get_parents
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/sample/parent
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
