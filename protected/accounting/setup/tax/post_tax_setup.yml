info:
  name: post_tax_setup
  type: http
  seq: 4

http:
  method: POST
  url: http://{{host}}:{{port}}/api/accounting/tax
  body:
    type: text
    data: |-
      {
          "code": "CODE AAAAA",
          "name": "NAME AAAAA"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
