info:
  name: get_coa_setup
  type: http
  seq: 3

http:
  method: GET
  url: http://{{host}}:{{port}}/api/accounting/tax/coa
  body:
    type: text
    data: |-
      {
          "code": "CODE AAAAA",
          "name": "NAME AAAAA"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
