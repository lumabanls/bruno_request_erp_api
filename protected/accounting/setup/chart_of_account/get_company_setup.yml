info:
  name: get_company_setup
  type: http
  seq: 5

http:
  method: GET
  url: http://{{host}}:{{port}}/api/accounting/company_setup
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
