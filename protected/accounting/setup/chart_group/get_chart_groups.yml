info:
  name: get_chart_groups
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/item/class
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
