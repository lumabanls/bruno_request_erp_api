info:
  name: get_invoice
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/accounting/sales_invoice_doc_no
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
