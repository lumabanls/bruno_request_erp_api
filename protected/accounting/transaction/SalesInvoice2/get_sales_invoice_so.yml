info:
  name: get_sales_invoice_so
  type: http
  seq: 5

http:
  method: GET
  url: http://{{host}}:{{port}}/api/accounting/customer_so/70300
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
