info:
  name: get_payment_receipt
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/accounting/payment_receipt
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
