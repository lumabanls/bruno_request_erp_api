info:
  name: get_bulk_invoice_receipt_search
  type: http
  seq: 5

http:
  method: GET
  url: http://{{host}}:{{port}}/api/accounting/bulk_invoice_receipt/search//new_add
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
