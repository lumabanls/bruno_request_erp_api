info:
  name: delete_journal_entry
  type: http
  seq: 6

http:
  method: DELETE
  url: http://{{host}}:{{port}}/api/accounting/journal_entry
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
