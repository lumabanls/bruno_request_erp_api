info:
  name: get_journal_entry
  type: http
  seq: 3

http:
  method: GET
  url: http://{{host}}:{{port}}/api/accounting/journal_entry2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
