info:
  name: get_invoice_receipt
  type: http
  seq: 1

http:
  method: POST
  url: http://{{host}}:{{port}}/api/accounting/invoice_receipt/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
