info:
  name: put_job_order
  type: http
  seq: 5

http:
  method: PUT
  url: http://{{host}}:{{port}}/api/engineering/job_order
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
