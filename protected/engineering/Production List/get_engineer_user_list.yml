info:
  name: get_engineer_user_list
  type: http
  seq: 6

http:
  method: GET
  url: http://{{host}}:{{port}}/api/engineering/job_order/engr_list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
