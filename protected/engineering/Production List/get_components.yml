info:
  name: get_components
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/engineering/job_order/components/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
