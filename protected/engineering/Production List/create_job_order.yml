info:
  name: create_job_order
  type: http
  seq: 4

http:
  method: POST
  url: http://{{host}}:{{port}}/api/engineering/job_order
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
