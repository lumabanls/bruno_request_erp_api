info:
  name: PickActivity_get
  type: http
  seq: 1

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/pickAct/list
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
