info:
  name: BinLocation_get
  type: http
  seq: 2

http:
  method: GET
  url: http://{{host}}:{{port}}/api/setup/pickAct/binloc
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
