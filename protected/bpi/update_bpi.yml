info:
  name: update_bpi
  type: http
  seq: 6

http:
  method: PUT
  url: http://{{host}}:{{port}}/api/bpi
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
