info:
  name: get_bpi_list
  type: http
  seq: 8

http:
  method: GET
  url: http://{{host}}:{{port}}/api/bpi
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
