info:
  name: check_health
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/hello"
  auth: inherit

runtime:
  variables:
    - name: port
      value: "3000"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
