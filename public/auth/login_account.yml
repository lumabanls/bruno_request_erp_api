info:
  name: login_account
  type: http
  seq: 2

http:
  method: POST
  url: http://{{host}}:{{port}}/api/login
  body:
    type: json
    data: |-
      {
          "employee_id": "IT-FD-3",
          "password": "IT-FD-3",
          "motherboard_serial_no": "macserial",
          "machine_name": "mac_mini"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
